import { ImageStyle } from 'react-native';

const Styles = {
  tabIconStyle: {
    width: 22,
    height: 22,
    resizeMode: 'contain',
  } as ImageStyle,
  activeTab: {
    opacity: 1,
  },
  inActiveTab: {
    opacity: 0.5,
  },
};

export default Styles;
